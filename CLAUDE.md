# desaign-team-garage — CLAUDE.md

非エンジニアのデザイナーが AI・GitHub・Frontend の基礎を覚えるためのリポジトリ。

## メモリの保存先

このリポジトリ内でのメモリ（セッション要約・記録）は `.agent/memory/` に残す。
グローバルの memory システム（`~/.claude/projects/.../memory/`）は使わない。

## プランの保存先

プランモードで作成した plan.md は以下の両方に保存する：

1. グローバル: `~/.claude/plans/`
2. このワークスペース: `.agent/plans/`（`./.claude/plans/` ではなくここ）
